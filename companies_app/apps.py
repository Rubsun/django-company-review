"""App configuration for the companies application."""

from django.apps import AppConfig


class CompaniesAppConfig(AppConfig):
    """App configuration for the companies application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'companies_app'
